# This is a secret key used by Auth.js to sign tokens.
# You can generate a good one by running `openssl rand -base64 32` in your terminal.
AUTH_SECRET="your-super-secret-auth-key-goes-here"

# This is the connection string for your PostgreSQL database in Docker.
POSTGRES_URL="postgresql://euplan_db_admin:Al3xandru@localhost:5432/euplan_db"